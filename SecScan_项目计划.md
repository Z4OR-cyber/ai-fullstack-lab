# SecScan — AI 驱动的代码安全审计平台

> 项目目标：通过构建一个真实可部署的项目，补全"项目经验(15%)"和"DevOps实操(35%)"两大缺口
> 创建时间：2026-08-05

---

## 一、为什么是这个项目

### 缺口匹配

| 缺口 | 当前 | 目标 | 本项目如何补全 |
|------|------|------|---------------|
| 项目经验 | 15% | 70%+ | 从零构建完整项目，经历需求→设计→开发→测试→部署全流程 |
| DevOps实操 | 35% | 70%+ | Docker容器化 + Docker Compose编排 + K8s部署 + GitHub Actions CI/CD |
| RAG实战 | 0% | 80%+ | 集成RAG知识库，检索安全知识生成修复建议 |
| 安全实战 | 75% | 85%+ | 安全审计引擎是核心功能，结合Bug Bounty知识库 |

### 技能联动

```
轨道1 编程语言 ──→ Python(FastAPI后端) + TypeScript(前端)
轨道2 AI/数学  ──→ RAG知识检索 + Embedding向量搜索
轨道3 工程全栈 ──→ RESTful API + PostgreSQL + 前端UI + 设计模式
轨道4 安全攻防 ──→ 代码安全审计引擎 + Bug Bounty知识库
新缺口 DevOps  ──→ Docker + Compose + K8s + CI/CD + 监控
```

**一个项目联动 5 条能力轨道，这是最高效的补缺方式。**

---

## 二、项目架构

### 2.1 系统架构图

```
                    ┌─────────────────────────────────────────┐
                    │              用户浏览器                    │
                    │  上传代码 → 查看报告 → 下载报告             │
                    └──────────────────┬──────────────────────┘
                                       │ HTTP
                    ┌──────────────────▼──────────────────────┐
                    │           Nginx (反向代理)                │
                    │        TLS + 静态文件 + 负载均衡           │
                    └──────────────────┬──────────────────────┘
                                       │
                    ┌──────────────────▼──────────────────────┐
                    │         FastAPI 后端 (Python)             │
                    │                                         │
                    │  ┌──────────┐  ┌──────────┐  ┌────────┐│
                    │  │ 文件处理  │  │ 审计引擎  │  │ RAG检索 ││
                    │  │ 模块     │  │ 模块     │  │ 模块   ││
                    │  └──────────┘  └────┬─────┘  └───┬────┘│
                    │                     │             │      │
                    │              ┌──────▼─────┐ ┌────▼────┐ │
                    │              │  漏洞检测   │ │ 向量检索 │ │
                    │              │  AST分析    │ │ 知识匹配 │ │
                    │              │  规则引擎    │ │ 修复建议 │ │
                    │              └──────┬─────┘ └────┬────┘ │
                    │                     │             │      │
                    │              ┌──────▼─────────────▼────┐ │
                    │              │    报告生成器            │ │
                    │              │  (JSON/HTML/PDF)        │ │
                    │              └─────────────────────────┘ │
                    └─────────┬─────────────┬──────────────────┘
                              │             │
                    ┌─────────▼─────┐ ┌────▼──────────────┐
                    │  PostgreSQL    │ │   ChromaDB        │
                    │  (审计记录)    │ │  (安全知识向量库)  │
                    │  + pgvector    │ │                   │
                    └───────────────┘ └───────────────────┘
```

### 2.2 技术栈

| 层级 | 技术 | 已学衔接 | 新学内容 |
|------|------|----------|----------|
| 前端 | HTML/CSS/JS + Chart.js | 阶段四(30题) | 实际页面交互 |
| 后端 | FastAPI + Uvicorn | 阶段四(30题) | 异步任务队列 |
| 安全引擎 | Python AST + 正则 | 阶段九(15题) | 静态分析实战 |
| RAG | ChromaDB + numpy | RAG 10题(创建中) | 生产级RAG部署 |
| 数据库 | PostgreSQL + SQLAlchemy | 阶段四+六(40题) | pgvector向量存储 |
| 容器化 | Docker + Compose | DevOps 5题(创建中) | 生产级Docker部署 |
| 编排 | Kubernetes | DevOps 5题(创建中) | K8s实战部署 |
| CI/CD | GitHub Actions | DevOps 5题(创建中) | 自动化流水线 |
| 监控 | Prometheus + Grafana | DevOps 5题(创建中) | 可观测性实战 |

---

## 三、开发阶段（6阶段）

### 阶段1：核心 API + 安全审计引擎（3-5天）

**目标**：构建可用的代码安全审计 API

**功能**：
- 接收上传的 Python/JavaScript 代码文件
- AST 解析 + 正则规则匹配
- 检测 10+ 种漏洞类型（SQL注入/XSS/硬编码密钥/路径遍历/命令注入等）
- 返回 JSON 格式的审计结果

**技能联动**：Python(轨道1) + 安全攻击(轨道4)

**产出**：
```
secscan/
├── app/
│   ├── main.py          # FastAPI 入口
│   ├── api/
│   │   ├── scan.py      # 审计 API
│   │   └── report.py    # 报告 API
│   ├── engine/
│   │   ├── analyzer.py  # AST 分析器
│   │   ├── rules.py     # 漏洞规则定义
│   │   └── scanner.py   # 扫描调度器
│   └── models/
│       └── scan_result.py  # 数据模型
├── tests/
│   ├── test_scanner.py
│   └── samples/         # 测试用漏洞代码
└── requirements.txt
```

### 阶段2：RAG 知识库集成（2-3天）

**目标**：为每个检测到的漏洞提供知识库检索和修复建议

**功能**：
- ChromaDB 存储安全知识（Bug Bounty 知识库内容）
- 检测到漏洞后自动检索相关知识
- 生成修复建议（基于知识库内容 + 漏洞上下文）
- 支持中文知识检索

**技能联动**：AI/数学(轨道2) + RAG 10题

**产出**：
```
secscan/
├── app/
│   ├── rag/
│   │   ├── knowledge_base.py  # 知识库管理
│   │   ├── retriever.py       # 检索器
│   │   └── advisor.py         # 修复建议生成
│   └── data/
│       └── security_kb/       # 安全知识文档
```

### 阶段3：Web 前端（3-5天）

**目标**：构建用户友好的 Web 界面

**功能**：
- 代码上传页面（拖拽 + 粘贴）
- 实时扫描进度显示
- 审计结果可视化（漏洞列表 + 严重程度图表）
- 漏洞详情页（代码高亮 + 修复建议）
- 报告导出（PDF/HTML）

**技能联动**：全栈开发(轨道3) + TypeScript(轨道1)

**产出**：
```
secscan/
├── frontend/
│   ├── index.html       # 主页面
│   ├── styles.css       # 样式
│   ├── app.js           # 交互逻辑
│   └── components/      # UI组件
```

### 阶段4：数据库 + 持久化（2-3天）

**目标**：审计记录持久化存储

**功能**：
- PostgreSQL 存储审计记录
- SQLAlchemy ORM 模型
- 审计历史查询
- 用户/项目概念（可选）

**技能联动**：数据库工程(轨道3)

### 阶段5：Docker + CI/CD 部署（2-3天）

**目标**：生产级容器化部署

**功能**：
- 多阶段 Dockerfile
- Docker Compose（app + postgres + chromadb）
- GitHub Actions CI/CD（测试→构建→推送镜像）
- K8s 部署 manifest

**技能联动**：DevOps 5题(新缺口)

### 阶段6：测试 + 文档 + 发布（2-3天）

**目标**：项目可维护、可发布

**功能**：
- pytest 单元测试 + 集成测试
- API 文档（FastAPI 自动生成 Swagger）
- README + 部署文档
- GitHub 开源发布

**技能联动**：工程进阶(轨道3) + 输出阶段(仓库升级)

---

## 四、时间线

| 阶段 | 内容 | 天数 | 前置条件 | 可并行 |
|------|------|------|----------|--------|
| 1 | 核心API + 审计引擎 | 3-5 | 无 | RAG/DevOps练习完成后 |
| 2 | RAG知识库集成 | 2-3 | RAG 10题完成 | - |
| 3 | Web前端 | 3-5 | 阶段1完成 | 阶段2可并行 |
| 4 | 数据库持久化 | 2-3 | 阶段1完成 | 阶段2/3可并行 |
| 5 | Docker + CI/CD | 2-3 | DevOps 5题完成 | 阶段6可并行 |
| 6 | 测试 + 文档 | 2-3 | 全部完成 | - |

**总计**：14-22天（可压缩到 10-15天如果并行推进）

---

## 五、能力缺口补全预估

| 缺口 | 当前 | 项目完成后 | 补全方式 |
|------|------|-----------|----------|
| 项目经验 | 15% | 75%+ | 完整项目开发全流程 |
| DevOps实操 | 35% | 70%+ | Docker + Compose + K8s + CI/CD + 监控 |
| RAG实战 | 0% | 80%+ | 生产级RAG集成 |
| 全栈开发 | 65% | 80%+ | 前后端 + 数据库实战 |
| 安全实战 | 75% | 85%+ | 安全引擎开发 |

**项目完成后整体能力从 ~65% 提升到 ~80%+**

---

## 六、与双轨计划的关系

```
双轨并行（本周）                    三阶段输出（后续）
┌─────────────────────┐          ┌──────────────────┐
│ 轨道A: Bug Bounty   │          │ 1. 代码Cookbook   │
│ 轨道B: RAG 10题     │──完成后──→│ 2. 仓库升级       │
│ + DevOps 5题        │          │ 3. 技术博客       │
└─────────────────────┘          └──────────────────┘
         │                                ↑
         ▼                                │
┌─────────────────────┐                   │
│ SecScan 项目开发     │───完成后───────────┘
│ (6阶段, 14-22天)    │
└─────────────────────┘
```

SecScan 项目是双轨练习的**实战整合**：
- RAG 10题的知识 → 阶段2 RAG集成
- DevOps 5题的配置 → 阶段5 容器化部署
- 安全攻防25题的知识 → 阶段1 审计引擎
- 全栈开发30题的知识 → 阶段3 Web前端
- 数据库工程10题的知识 → 阶段4 持久化

---

> 更新记录：
> - 2026-08-05：创建项目计划。等待 RAG + DevOps 练习完成后启动开发
